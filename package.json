{
  "name": "richup",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "dev:web": "turbo dev --filter=@richup/web",
    "dev:api": "turbo dev --filter=@richup/api",
    "dev:all": "turbo dev --filter=@richup/web --filter=@richup/api"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@9.15.0"
}
